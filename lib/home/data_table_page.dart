import 'package:flutter/material.dart';

class DataTablePage extends StatelessWidget {
  const DataTablePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: implementar la Data table con CRUD incluido de documentos Firebase (Flutter Fire UI)
    return Scaffold(
      appBar: AppBar(
        title: Text("Data table firebase"),
      ),
      body: Center(
        child: Text(
            "TODO: implementar la Data table con CRUD incluido de documentos"),
      ),
    );
  }
}
// https://firebase.flutter.dev/docs/ui/widgets
