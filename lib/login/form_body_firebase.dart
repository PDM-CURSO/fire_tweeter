import 'package:flutter/material.dart';

class FormBodyFirebase extends StatelessWidget {
  FormBodyFirebase({
    Key? key,
  }) : super(key: key);
  // TODO: Configurar widget Login page de Flutter Fire
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("TODO: Implementar el login pre hecho de Flutter Fire UI"),
    );
  }
}

// https://firebase.flutter.dev/docs/ui/auth
